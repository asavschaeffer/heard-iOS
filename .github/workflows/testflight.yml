name: Build and Deploy to TestFlight

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      release_notes:
        description: 'Release notes for TestFlight'
        required: false
        default: 'Automated build from GitHub Actions'

jobs:
  build-and-deploy:
    runs-on: macos-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
          
      - name: Install CocoaPods (if needed)
        run: |
          cd HeardChef
          if [ -f "Podfile" ]; then
            pod install
          fi
          
      - name: Decode Secrets
        env:
          SECRETS_CONTEXT: ${{ toJson(secrets) }}
        run: |
          echo "Available secrets:"
          echo "$SECRETS_CONTEXT" | jq -r 'keys[]'
          
      - name: Build Archive
        run: |
          cd HeardChef
          echo "Building iOS archive..."
          
          # Build the archive
          xcodebuild -project HeardChef.xcodeproj \
                    -scheme heard \
                    -configuration Release \
                    -sdk iphoneos \
                    -destination generic/platform=iOS \
                    -archivePath $PWD/build.xcarchive \
                    archive
                    
          echo "Archive completed successfully!"
          
      - name: Export IPA
        run: |
          cd HeardChef
          echo "Exporting IPA from archive..."
          
          # Create export options
          cat > ExportOptions.plist << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store-connect</string>
              <key>teamID</key>
              <string>9L6Y9T3PPW</string>
              <key>destination</key>
              <string>export</string>
              <key>uploadBitcode</key>
              <false/>
              <key>uploadSymbols</key>
              <true/>
          </dict>
          </plist>
          EOF
          
          # Export the IPA
          xcodebuild -exportArchive \
                    -archivePath $PWD/build.xcarchive \
                    -exportPath $PWD/build \
                    -exportOptionsPlist ExportOptions.plist \
                    -allowProvisioningUpdates
                    
          echo "IPA export completed!"
          ls -la build/
          
      - name: Upload to TestFlight
        if: github.event_name == 'push'
        uses: apple-actions/upload-testflight-build@v1
        with:
          app-path: HeardChef/build/heard.ipa
          issuer-id: ${{ secrets.APPSTORE_ISSUER_ID }}
          api-key: ${{ secrets.APPSTORE_API_KEY }}
          api-key-id: ${{ secrets.APPSTORE_API_KEY_ID }}
          
      - name: Upload to TestFlight (Manual)
        if: github.event_name == 'workflow_dispatch'
        uses: apple-actions/upload-testflight-build@v1
        with:
          app-path: HeardChef/build/heard.ipa
          issuer-id: ${{ secrets.APPSTORE_ISSUER_ID }}
          api-key: ${{ secrets.APPSTORE_API_KEY }}
          api-key-id: ${{ secrets.APPSTORE_API_KEY_ID }}
          changelog: ${{ github.event.inputs.release_notes || 'Automated build from GitHub Actions' }}
          
      - name: Notify Success
        if: success()
        run: |
          echo "🎉 Build and deployment successful!"
          echo "📱 Check TestFlight: https://appstoreconnect.apple.com/apps"
          echo "🔄 Your app will be available on your device shortly"
          
      - name: Archive Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: |
            HeardChef/build/heard.ipa
            HeardChef/build/heard.app.dSYM.zip
          retention-days: 30